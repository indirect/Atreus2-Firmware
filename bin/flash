#!/bin/bash
set -v

make maybe_build

stty -f /dev/cu.usbmodemCatreusE1 1200
sleep 3

export DEVICE_PORT_BOOTLOADER=$(ls /dev/cu.usbmodem* | grep -v usbmodemC)
[[ -n "$DEVICE_PORT_BOOTLOADER" ]] && make flash || echo "Couldn't find a device for flashing!"
